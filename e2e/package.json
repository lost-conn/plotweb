{
  "name": "plotweb-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for PlotWeb (Playwright).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "test:cutover": "PLOTWEB_E2E_CUTOVER=* playwright test cutover-sync"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
